# Images for LinkedIn posts

Drop image files here (JPG or PNG, any name) — the 09:00 PKT post will
attach one, rotating through whatever's in this folder in alphabetical
order. The 18:00 PKT post always stays text-only.

If this folder is empty, every post is text-only (no error, it just skips
the image step).
